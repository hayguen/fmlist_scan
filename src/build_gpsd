
BASE=$(pwd)

cd ${BASE}

if [ ! -f gpsd-master.tar.gz ]; then
  echo "getting latest gpsd-master.tar.gz to $(pwd) .."
  wget http://gitlab.com/gpsd/gpsd/-/archive/master/gpsd-master.tar.gz

fi

tar zxf gpsd-master.tar.gz
cd gpsd-master/

scons
scons check

cd ${BASE}
